This package must be filled with boost code in order to be able to work
Download latest version from : https://www.boost.org/users/download/

unzip 'boost' directory directly into boost package
==> you should get    boost/boost/<boost src files>

Note : you can also unzip lib directory (but you may have some additional build steps) -- NOT TESTED --